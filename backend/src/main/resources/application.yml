server:
  port: 8080

spring:
  application:
    name: estudos-api
  data:
    mongodb:
      # Conexão única via URI. Por padrão aponta para o cluster do MongoDB Atlas.
      # Pode ser sobrescrita pela variável de ambiente MONGO_URI.
      uri: ${MONGO_URI:mongodb+srv://jdavidjl97_db_user:6nWHwIqhNfq6cpTP@cluster0.frz73kr.mongodb.net/estudos_db?retryWrites=true&w=majority&appName=Cluster0}
      auto-index-creation: true

logging:
  level:
    com.estudos: INFO
    org.springframework.data.mongodb: INFO
