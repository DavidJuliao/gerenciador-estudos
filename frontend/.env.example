# Copie para ".env" (não versionado) para desenvolvimento local, se quiser.
# Em produção (Vercel/Netlify/Cloudflare), defina VITE_API_URL no painel do host.
#
# URL pública do backend. Sem isso, o front usa http://localhost:8080 (fallback).
VITE_API_URL=http://localhost:8080
